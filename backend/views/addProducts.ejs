<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head.ejs") %>
    <title>Add Product</title>
    <style>
      .main-content {
        margin-left: 250px;
        padding: 20px;
      }
      .variant-section {
        border: 1px solid #ddd;
        padding: 15px;
        margin-bottom: 20px;
        border-radius: 5px;
      }
      .add-variant-btn {
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <%- include("./partials/nav.ejs") %>

    <div class="main-content">
      <div class="container-fluid mt-4">
        <h1>Add Product</h1>

        <!-- Product Details Section -->
        <form
          action="/api/v1/product/add"
          method="POST"
          enctype="multipart/form-data"
        >
          <div class="card mb-4">
            <div class="card-header">Product Details</div>
            <div class="card-body">
              <div class="form-group">
                <label for="productName">Product Name</label>
                <input
                  type="text"
                  class="form-control"
                  id="productName"
                  name="productName"
                  required
                />
              </div>
              <div class="form-group">
                <label for="productDescription">Description</label>
                <textarea
                  class="form-control"
                  id="productDescription"
                  name="productDescription"
                  rows="3"
                  required
                ></textarea>
              </div>

              <div class="form-group">
                <label for="category">Category</label>
                <select
                  multiple
                  class="form-control"
                  id="category"
                  name="category"
                  required
                >
                  <option value="">Select Category</option>
                </select>
              </div>
            </div>
          </div>

          <!-- Variant Section -->
          <div class="card mb-4">
            <div class="card-header">Variants</div>
            <div class="card-body" id="variantContainer">
              <!-- Initial Variant -->
              <div class="variant-section">
                <div class="form-row">
                  <div class="form-group col-md-3">
                    <label for="price">Price</label>
                    <input
                      type="number"
                      class="form-control"
                      name="variants[0][price]"
                      required
                    />
                  </div>
                  <div class="form-group col-md-3">
                    <label for="color">Color</label>
                    <input
                      type="text"
                      class="form-control"
                      name="variants[0][color]"
                      required
                    />
                  </div>
                  <div class="form-group col-md-3">
                    <label for="size">Size</label>
                    <input
                      type="text"
                      class="form-control"
                      name="variants[0][size]"
                      required
                    />
                  </div>
                  <div class="form-group col-md-3">
                    <label for="stock">Stock</label>
                    <input
                      type="number"
                      class="form-control"
                      name="variants[0][stock]"
                      required
                    />
                  </div>
                </div>
                <div class="form-row">
                  <div class="form-group col-md-6">
                    <label for="sku">SKU</label>
                    <input
                      type="text"
                      class="form-control"
                      name="variants[0][sku]"
                      required
                    />
                  </div>
                  <div class="form-group col-md-6">
                    <label for="images">Images (Max 5)</label>
                    <input
                      type="file"
                      class="form-control"
                      name="variants[0][images]"
                      multiple
                      accept="image/*"
                      max="5"
                      required
                    />
                  </div>
                </div>
                <button
                  type="button"
                  class="btn btn-danger btn-sm remove-variant-btn"
                >
                  Remove Variant
                </button>
              </div>
            </div>
            <div class="card-footer">
              <button
                type="button"
                class="btn btn-primary add-variant-btn"
                id="addVariant"
              >
                Add Variant
              </button>
            </div>
          </div>

          <!-- Submit Button -->
          <button type="submit" class="btn btn-success">Submit</button>
        </form>
      </div>
    </div>

    <%- include("./partials/script.ejs") %>

    <!-- JavaScript for Dynamic Variant Addition -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        fetch("/api/v1/category")
          .then((response) => response.json())
          .then((data) => {
            const categorySelect = document.getElementById("category");
            data.forEach((category) => {
              const option = document.createElement("option");
              option.value = category.id;
              option.text = category.categoryName;
              categorySelect.appendChild(option);
            });
          })
          .catch((error) => console.error("Error fetching categories:", error));
      });

      document.addEventListener("DOMContentLoaded", function () {
        const variantContainer = document.getElementById("variantContainer");
        const addVariantBtn = document.getElementById("addVariant");

        let variantCount = 1; // Start counting from 1 since the first variant is already present

        // Function to create a new variant section
        function createVariantSection(index) {
          const newVariant = document.createElement("div");
          newVariant.classList.add("variant-section");

          newVariant.innerHTML = `
                  <div class="form-row">
                    <div class="form-group col-md-3">
                      <label for="price">Price</label>
                      <input
                        type="number"
                        class="form-control"
                        name="variants[${index}][price]"
                        required
                      />
                    </div>
                    <div class="form-group col-md-3">
                      <label for="color">Color</label>
                      <input
                        type="text"
                        class="form-control"
                        name="variants[${index}][color]"
                        required
                      />
                    </div>
                    <div class="form-group col-md-3">
                      <label for="size">Size</label>
                      <input
                        type="text"
                        class="form-control"
                        name="variants[${index}][size]"
                        required
                      />
                    </div>
                    <div class="form-group col-md-3">
                      <label for="stock">Stock</label>
                      <input
                        type="number"
                        class="form-control"
                        name="variants[${index}][stock]"
                        required
                      />
                    </div>
                  </div>
                  <div class="form-row">
                    <div class="form-group col-md-6">
                      <label for="sku">SKU</label>
                      <input
                        type="text"
                        class="form-control"
                        name="variants[${index}][sku]"
                        required
                      />
                    </div>
                    <div class="form-group col-md-6">
                      <label for="images">Images (Max 5)</label>
                      <input
                        type="file"
                        class="form-control"
                        name="variants[${index}][images]"
                        multiple
                        accept="image/*"
                        max="5"
                        required
                      />
                    </div>
                  </div>
                  <button
                    type="button"
                    class="btn btn-danger btn-sm remove-variant-btn"
                  >
                    Remove Variant
                  </button>
                `;

          // Add event listener to the remove button
          const removeBtn = newVariant.querySelector(".remove-variant-btn");
          removeBtn.addEventListener("click", function () {
            newVariant.remove();
          });

          return newVariant;
        }

        // Add Variant Button Click Event
        addVariantBtn.addEventListener("click", function () {
          const newVariant = createVariantSection(variantCount);
          variantContainer.appendChild(newVariant);
          variantCount++;
        });

        // Remove Variant Functionality for Initial Variant
        const removeButtons = document.querySelectorAll(".remove-variant-btn");
        removeButtons.forEach((button) => {
          button.addEventListener("click", function () {
            button.closest(".variant-section").remove();
          });
        });
      });
    </script>
  </body>
</html>

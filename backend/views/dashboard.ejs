<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head.ejs") %>
    <title>Admin Dashboard</title>
    <style>
      body {
        display: flex;
        min-height: 100vh;
      }
      .sidebar {
        width: 250px;
        height: 100vh;
        background-color: #343a40;
        color: white;
        padding-top: 20px;
        position: fixed;
        left: 0;
        top: 0;
      }
      .sidebar a {
        display: block;
        color: white;
        padding: 10px 20px;
        text-decoration: none;
      }
      .sidebar a:hover {
        background-color: #495057;
      }
      .content {
        margin-left: 250px;
        padding: 20px;
        flex-grow: 1;
      }
    </style>
  </head>
  <body>
    <%- include("./partials/nav") %>

    <!-- Main Content -->
    <div class="content">
      <h1>Welcome, <%= user.email %>!</h1>
      <p>Manage your admin panel efficiently.</p>
    </div>

    <%- include("./partials/script") %>
  </body>
</html>
